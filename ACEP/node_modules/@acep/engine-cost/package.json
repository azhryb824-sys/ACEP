{
  "name": "@acep/engine-cost",
  "version": "1.0.0",
  "description": "Cost Engine - Calculates project costs with dynamic pricing and scenario analysis",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "jest" },
  "dependencies": {
    "@acep/core": "^1.0.0",
    "@acep/knowledge-base": "^1.0.0"
  }
}
