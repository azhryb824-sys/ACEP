{
  "name": "@acep/ai-embeddings",
  "version": "1.0.0",
  "description": "Vector Embedding Service - Real embedding API (OpenAI/Together) + in-memory vector store with cosine similarity for ACEP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
