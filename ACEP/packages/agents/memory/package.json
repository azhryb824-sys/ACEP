{
  "name": "@acep/agent-memory",
  "version": "1.0.0",
  "description": "Memory Agent - Learns from user decisions over time",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": { "build": "tsc", "test": "jest" },
  "dependencies": {
    "@acep/core": "^1.0.0"
  }
}
