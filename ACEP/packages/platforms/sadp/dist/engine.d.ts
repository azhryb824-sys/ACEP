import { BaseEngine } from '@acep/core';
import type { IDeveloperPlatform, IAPIManager, IPluginFramework, ISDKManager, IWebhookManager, IMarketplaceManager, IDocumentationService } from './interfaces';
import type { APIEndpoint, APISecurity, DeveloperEnvironment, DeveloperPortal, DeveloperTrustIndex, DeveloperProfile, DeveloperMetrics, ConnectorConfig } from './types';
import { EventType } from './types';
export declare class DeveloperPlatform extends BaseEngine implements IDeveloperPlatform {
    private apiManager;
    private pluginFramework;
    private sdkResults;
    private webhooks;
    private deliveryLogs;
    private marketplaceListings;
    private marketplaceReviews;
    private profiles;
    private connectors;
    private developerPortal;
    private developerEnvironment;
    private apiSecurity;
    private eventListeners;
    constructor(config?: Record<string, unknown>);
    initialize(): Promise<void>;
    validate(): Promise<boolean>;
    getAPIManager(): IAPIManager;
    getPluginFramework(): IPluginFramework;
    getSDKManager(): ISDKManager;
    getWebhookManager(): IWebhookManager;
    getMarketplaceManager(): IMarketplaceManager;
    getDocumentationService(): IDocumentationService;
    getDeveloperPortal(): Promise<DeveloperPortal>;
    updateDeveloperPortal(config: Partial<DeveloperPortal>): Promise<DeveloperPortal>;
    getDeveloperEnvironment(): Promise<DeveloperEnvironment>;
    configureDeveloperEnvironment(config: Partial<DeveloperEnvironment>): Promise<DeveloperEnvironment>;
    getAPISecurity(): Promise<APISecurity>;
    updateAPISecurity(config: Partial<APISecurity>): Promise<APISecurity>;
    getDeveloperTrustIndex(developerId: string): Promise<DeveloperTrustIndex>;
    getDeveloperProfile(developerId: string): Promise<DeveloperProfile>;
    getDeveloperMetrics(developerId: string): Promise<DeveloperMetrics>;
    registerConnector(config: ConnectorConfig): Promise<ConnectorConfig>;
    getConnectors(): Promise<ConnectorConfig[]>;
    removeConnector(connectorId: string): Promise<void>;
    getAPIEndpoints(): Promise<APIEndpoint[]>;
    publishEvent(event: EventType, payload: Record<string, unknown>): Promise<void>;
    subscribeToEvent(event: EventType, handler: (payload: Record<string, unknown>) => Promise<void>): Promise<void>;
    private generateSDK;
    private listGeneratedSDKs;
    private getSDKPackage;
    private deleteSDKPackage;
    private getSDKConfig;
    private updateSDKConfig;
    private generateClientCode;
    private registerWebhook;
    private updateWebhook;
    private removeWebhook;
    private getWebhook;
    private listWebhooks;
    private triggerEvent;
    private getDeliveryLogs;
    private testWebhook;
    private getWebhookSecret;
    private rotateWebhookSecret;
    private publishListing;
    private updateListing;
    private unpublishListing;
    private getMarketplaceListing;
    private searchMarketplace;
    private getByCategory;
    private getFeaturedListings;
    private submitForReview;
    private approveMarketplaceListing;
    private rejectMarketplaceListing;
    private addMarketplaceReview;
    private getMarketplaceReviews;
    private getMarketplaceInstallCount;
    private incrementMarketplaceInstallCount;
    private generateAPIDocs;
    private generateSDKDocs;
    private generateChangelog;
    private getEndpointDocs;
    private searchDocumentation;
    registerDeveloper(profile: DeveloperProfile): void;
    updateDeveloperTrustIndex(developerId: string, updates: Partial<DeveloperTrustIndex>): Promise<DeveloperTrustIndex>;
}
//# sourceMappingURL=engine.d.ts.map